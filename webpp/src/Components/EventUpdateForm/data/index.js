var eventToBeEdited;
var eventToBeEditedES;

export const setEventToBeEdited = (event) => {
    eventToBeEdited = event;
}

export const getEventToBeEdited = () => {
    return eventToBeEdited
}

export const setEventToBeEditedES = (event) => {
    eventToBeEditedES = event;
}

export const getEventToBeEditedES = () => {
    return eventToBeEditedES
}